const sumRange = require('./sumRange');

describe('sumRange', () => {
  test('First test description', () => {
    

    expect(sumRange()).toBe('');
  });
  
  test.skip('Second test description', () => {


    expect(sumRange()).toBe('');
  });
});
